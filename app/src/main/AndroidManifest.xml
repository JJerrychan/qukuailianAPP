<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cq.xinyupintai">

    <application
        android:icon="@mipmap/logo"
        android:roundIcon="@mipmap/logo"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Presenter.activity.BossActivity"></activity>
        <activity android:name=".Presenter.activity.ClosingStockActivity" />
        <activity android:name=".Presenter.activity.register" />


        <activity android:name=".Presenter.EditCard"></activity>
        <activity android:name=".Presenter.register" />
        <activity
            android:name=".Presenter.MainView"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />


</manifest>